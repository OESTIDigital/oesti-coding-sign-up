<%= render 'page_header' %>
<%= render 'application/messages' %>

<%= form_for(@exercise) do |f| %>
	<div class="form-group">
    <%= f.label :title %>
	  <p class="form-control-static"><%= @exercise.title %></p>
	</div>

	<div class="form-group">
    <%= f.label :text %>
	  <p class="form-control-static"><%= @exercise.text %></p>
	</div>
<% end %>