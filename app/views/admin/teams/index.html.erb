<%= render 'page_header' %>
<%= render 'application/messages' %>

<table class="table">
  <thead>
    <tr>
      <th><%= I18n.t('helpers.label.team.name') %></th>
      <th class="actions"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td>
          <div class="btn-group">
            <%= link_to I18n.t('actions.show'), [:admin, team], class: 'btn btn-small btn-default' %>
            <%= link_to I18n.t('actions.edit'), edit_admin_team_path(team), class: 'btn btn-small btn-default' %>
            <%= link_to I18n.t('actions.destroy'), [:admin, team], method: :delete, class: 'btn btn-small btn-danger', data: { confirm: I18n.t('messages.are_you_sure') } %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
